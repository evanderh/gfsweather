services:

  server:
    build: server
    ports:
      - "8000:8000"
    environment:
      - ENV=production
    volumes:
      - ./server:/app

  tileserver:
    image: maptiler/tileserver-gl
    ports:
      - "8080:8080"
    volumes:
      - ./tileserver:/data
